<div class="col-md-offset-3 col-md-6">
  <h2>Prihlásenie</h2>

  <%= form_for(resource, :as => resource_name, :url => session_path(resource_name)) do |f| %>
    <div class="form-group">
      <%= f.label :login %>
      <%= f.text_field :login, class: 'form-control', :autofocus => true %>
    </div>

    <div class="form-group">
      <%= f.label :password %>
      <%= f.password_field :password, class: 'form-control' %>
    </div>

    <!-- TODO add remember me button-->

    <button type="submit" class="btn btn-primary btn-lg pull-right"><%= t('user.sign_in') %></button>

  <% end %>

  <!--TODO add proper links to the bottom of the form -->

</div>
